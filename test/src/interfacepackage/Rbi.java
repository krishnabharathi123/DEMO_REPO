package interfacepackage;

interface Rbi {
	void minimumBalance();// unimplemented or abstract method

	public abstract void maxLoanValue();// both methods are same, if u give only void it will take public abstrace by
										// default

	int minLoanValue(int a);
}
